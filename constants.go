package scs

// logic
const (
	AND = "and"
	OR = "or"
	IN = "in"
	NOT_IN = "not in"
	EXISTS = "exists"
	NOT_EXISTS = "not exists"
	LIKE = "like"
	NOT_LIKE = "not like"
)

// symbol
const (
	EQ = "="
	NOT_EQ = "<>"
	LT_EQ = "<="
	GT_EQ = ">="
	LT = "<"
	GT = ">"
)

const EMPTY = ""
const ONE_SPACE = " "
const PLACEHOLDER = "?"

const ZERO = 0
const ONE = 1